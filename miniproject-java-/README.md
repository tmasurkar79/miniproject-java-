Java Tic-Tac-Toe (Swing)

Files:
- Java_tic_tac_toe.java — main Swing application
- run.bat — Windows script to compile and run

Run (Windows):

Open PowerShell or Command Prompt and run:

    cd "C:\Users\YASH MASURKAR\OneDrive\Documents\javaproject"
    ./run.bat

Notes:
- Requires a JDK installed and `javac`/`java` on PATH.
- This is a simple Swing program; running will open a GUI window.